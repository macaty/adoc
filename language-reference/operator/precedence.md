[aardio 文档](../../index.htm "aardio 编程语言文档首页")

# 操作符优先级

aardio 操作符优先级�?C系语言基本兼容，唯一的区别是：位运算符优先级略高于等式、不等式运算符。从而保证各组运算符的优先级更加有序、并容易记忆�?
![](../../icon/info.gif) 试图记住所有的运算符优先级是不必要的，建议在易于混淆优先级的位置用括号明确标明优先级�?
### 一、成员符、括号、乘�?
操作符优先级结合说明`..`0(最�?左向右访问全局变量�?
等价�?`global.``::`0(最�?左向右访问全局常量成员操作�?`.`

下标操作�?`[]`

直接下标 `[[]]`

调用操作�?`()`

元表操作�?`@`1左向右`.` �?`[]`�?`[[]]`

都是用于访问对象成员的操作符�?
调用操作符用于调用函�?
或类构造函数，例如

`print("参数")` �?)2左向右用于组合表达式并改变优先级。`**`3右向左乘方运�?
### 二、单目运算符

操作符优先级结合说明`!`4左向右逻辑非not5左向右逻辑非`-`6左向右取负`~`7左向右`~` 按位取反

### 三、算术运算符

操作符优先级结合说明`*` �?`/` �?`%` 取模8左向右`+` �?`-` �?左向�?
### 四、按位运算符

操作符优先级结合说明`<<` 按位左移

`>>` 按位右移

`>>>` 无符号右�?0左向右`&` 按位�?
`^` 按位异或

\| 按位�?1左向右C 语言中位运算符的优先�?
低于 `==`�?`!=`

### 五、连接运算符

操作符优先级结合说明`++`12右向左连接运算符

### 六、关系运算符

操作符优先级结合说明`<` 小于

`<=` 小于等于

`>=` 大于等于

`>` 大于13左向�?
### 七、逻辑运算�?
操作符优先级结合说明`==`

`!=`

`===`

`!==`14左向右等于、不等于、恒等、非恒等于`&&` and15左向右逻辑与\|\|

or16左向右逻辑或`?`17左向右逻辑与、该运算符类似于and但优先级更低`:`18左向右逻辑或、该运算符类似于or但优先级更低

### 八、赋值操作符

操作符优先级结合说明`=`

`+=`

`-=`

`*=`

`/=`

`&=` `^=`

\|=

`<<=`

`>>=`最低优先级由右向左aardio中赋值操作符不能用于表达式并返回�?只能用于独立的赋值语�?
![](../../icon/info.gif) 除了 `**` �?`++` 外所有的二元运算符都是左连接�?

[Markdown 格式](javascript:if(confirm('https://www.aardio.com/zh-cn/doc/language-reference/operator/precedence.md  \n\n���ļ��޷��� Teleport Ultra ����, ��Ϊ ��������Ŀ�ļ����͹淶�ڡ�  \n\n�����ڷ������ϴ�����?'))window.location='https://www.aardio.com/zh-cn/doc/language-reference/operator/precedence.md')

