[aardio 文档](../../index.htm "aardio 编程语言文档首页")

# inet.urlpart 库模块帮助文�?
## inet.urlpart 成员列表

### inet.urlpart.getHost(url)

返回url主机域名部分

### inet.urlpart.getLocation(url)

返回url尾部#号后面指定的锚点名称

包含#�?
### inet.urlpart.getPassword(url)

返回url中的登录密码

### inet.urlpart.getPort(url)

如果url中包含端口号则返回端口号

未明确指定则返回null

### inet.urlpart.getQuery(url)

返回url尾部?号后面的QueryString参数

不包�?�?
### inet.urlpart.getScheme(url)

返回url协议�?例如"http"

### inet.urlpart.getUser(url)

返回url中的登录用户�?
[Markdown 格式](javascript:if(confirm('https://www.aardio.com/zh-cn/doc/library-reference/inet/urlpart.md  \n\n���ļ��޷��� Teleport Ultra ����, ��Ϊ ��������Ŀ�ļ����͹淶�ڡ�  \n\n�����ڷ������ϴ�����?'))window.location='https://www.aardio.com/zh-cn/doc/library-reference/inet/urlpart.md')

